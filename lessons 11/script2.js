const text = document.querySelector('.block__text__p');
const button = document.querySelector('.block__button');

button.addEventListener("click", () => {text.classList.toggle ('chenge') });

console.log(text);
